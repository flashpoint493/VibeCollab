name: CI

on:
  push:
    branches: [master, main, dev]
  pull_request:
    branches: [master, main, dev]

permissions:
  contents: read

jobs:
  test:
    name: Python ${{ matrix.python-version }} on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
        python-version: ["3.10", "3.11", "3.12", "3.13"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev,llm]"

      - name: Show installed packages
        run: pip list --format=columns

      - name: Verify package import
        run: python -c "import vibecollab; print(vibecollab.__version__)"

      - name: Lint with ruff
        run: ruff check src/vibecollab/ tests/

      - name: Collect tests (dry run)
        run: pytest tests/ --collect-only -q 2>&1 | head -20 || true
        shell: bash

      - name: Run tests
        env:
          PYTHONIOENCODING: utf-8
          PYTHONUTF8: 1
        run: pytest tests/ --tb=long -q

      - name: Run tests with coverage
        if: success()
        env:
          PYTHONIOENCODING: utf-8
          PYTHONUTF8: 1
        run: pytest tests/ -q --tb=short --cov=vibecollab --cov-report=xml

      - name: Upload coverage to Codecov
        if: matrix.os == 'ubuntu-latest' && matrix.python-version == '3.13'
        uses: codecov/codecov-action@v4
        with:
          file: ./coverage.xml
          fail_ci_if_error: false

  build:
    name: Build package
    runs-on: ubuntu-latest
    needs: test

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install build tools
        run: pip install build

      - name: Build sdist and wheel
        run: python -m build

      - name: Verify package
        run: |
          pip install dist/*.whl
          vibecollab --help

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist/
