# VibeCollab Global Context

> Last updated: 2026-03-26
> Aggregated from: dev

## Project Status
- **Version**: v0.10.10
- **Current phase**: Bug fixes from user testing + release engineering
- **Active roles**: 1 (DEV)
- **Environment**: Python 3.10.10 verified; local `.venv` created with editable install
- **Protocol status**: `vibecollab onboard` and `vibecollab check` both pass
- **Project metadata**: `project.yaml` synced to real VibeCollab identity

## Role Status

### DEV (Development)
- **Last updated**: 2026-03-26
- **Current task**: release engineering, external QA follow-through, and onboarding/documentation polish

### Recently completed (this session)
- **Environment setup**: Created local `.venv` and installed current repo as editable `vibe-collab`
- **Protocol verification**: Ran `vibecollab onboard` and `vibecollab check` successfully
- **Metadata correction**: Replaced placeholder `MyProject v1.0` config values with real repository metadata
- **Generated artifact refresh**: Regenerated `CONTRIBUTING_AI.md` and refreshed `llms.txt`

## Active Tasks
- Run `vibecollab init` + `generate` + `check` on 3+ real external projects
- Verify rich panel rendering on Windows PowerShell/CMD/WSL
- Verify `onboard` / `next` output quality on large repositories
- Continue GitHub facade and release-engineering polish

## Technical Debt
- External project QA validation remains pending
- `events.jsonl` Windows file lock issue needs investigation
- MCP `onboard` tool call timeout needs follow-up after IDE restart
- `README.zh-CN.md` project structure section is outdated

---
*This file is auto-aggregated from role contexts*
