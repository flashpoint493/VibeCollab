kind: context
version: "1"
updated_at: "2026-04-25T14:30:00"
aggregated_from:
  - architect
  - dev
  - insight_collector

project_status:
  current_version: "v0.12.6"
  active_roles:
    - architect
    - dev
    - insight_collector
  milestone: "v0.12.0 YAML Data Layer + Workflows + Insight Automation — 25/25 items COMPLETE (v0.12.0 RELEASED)"

roles:
  - role: architect
    updated_at: "2026-04-25"
    current_task: "Package split architecture validation"
    progress: "Split package structure finalized. Cross-package dependency graph resolved. Core retains domain/models/utils/i18n/contrib; insights owns search+insight; cli owns all CLI entrypoints."
    pending_issues: null
    next_steps: "After user validation, approve PyPI publish + main repo modular-deps branch"

  - role: dev
    updated_at: "2026-04-25"
    current_task: "Split package build & import fix"
    progress: "Automated script scripts/fix_split_packages.py created. All relative imports rewritten. All 8 packages build and import successfully. Code pushed to GitHub."
    pending_issues: "Local validation by user before PyPI upload"
    next_steps: "Await validation feedback, then proceed to main repo branch switch"

  - role: insight_collector
    updated_at: "2026-04-13"
    current_task: "v0.12.4 RELEASED"
    progress: "v0.12.4 RELEASED: Insight-collect workflow enhanced with tags enumeration step. 60+ Insights indexed. Insight derivation chain fully operational."
    pending_issues: "Cross-project import/export verification (v0.13.0 scope)"
    next_steps: "Insight marketplace planning for v0.13.0"

collaboration: "See .vibecollab/roles/ for individual role contexts"

technical_debt:
  - role: dev
    description: "MCP onboard tool call timeout after IDE restart still needs investigation"
  - role: dev
    description: "README.zh-CN.md project structure section is outdated"
  - role: dev
    description: "Sub-package code duplication between vibecollab-core and vibecollab-tasks (task_manager, event_log, role, roadmap_parser, prd_manager) — acceptable for backward compat during transition"
  - role: insight_collector
    description: "Semantic index staleness (no auto-update on Insight change)"
  - role: insight_collector
    description: "Duplicate detection sensitivity threshold needs tuning"
