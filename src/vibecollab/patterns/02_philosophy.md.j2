{%- set phil = philosophy if philosophy else {} -%}
{%- set vibe = phil.get('vibe_development', {}) -%}
{%- set dq = phil.get('decision_quality', {}) -%}
{%- set target_rate = dq.get('target_rate', 0.9) -%}
# 一、核心理念

## 1.1 Vibe Development 哲学

> **最珍贵的是对话过程本身，不追求直接出结果，而是步步为营共同规划。**

本项目采用 **Vibe Development** 模式：
{% if vibe.get('enabled', True) -%}
{% for principle in vibe.get('principles', []) -%}
- {{ principle }}
{% endfor -%}
{% endif %}
## 1.2 决策质量观

> **大量决策，{{ (target_rate * 100) | int }}% 正确率，关键决策零失误**

项目是一系列决策的集合：
- 只有做对 {{ (target_rate * 100) | int }}% 以上的决策，项目才有望成功
- 关键决策容错数: {{ dq.get('critical_tolerance', 0) }}
- 因此每个 S/A 级决策都需要 **人机共同 Review**
{% if phil.get('long_term_dialogue', True) %}
## 1.3 长期对话工程观

这是一个**长期对话工程**，不是一次性任务：
- 对话是连续的，上下文需要被**持久化保存**
- 每次对话都在前次基础上**迭代推进**
- Git 提交历史记录了**思维演进过程**
- CONTRIBUTING_AI.md 是**活文档**，随项目成长
{% endif %}
---
